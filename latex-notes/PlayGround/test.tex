\documentclass[a4paper, 12pts]{article}
\usepackage{amssymb}
\usepackage{amsmath}
%\usepackage{mathtools}
\title{Sum-Class Symbols}

\begin{document}

\maketitle	
\iffalse
	\section{Sum}

%		$$\sum\limit^n_{i=1} a_i$$
		$$\sum_{i=1}^na_i$$
		$$\int_{a}^b f(x)$$

		$$\prod_{i=1}^n p_i$$
		$$\sum \limits_a^b a_i$$
		$$\prod \limits_a^b a_i$$
		$$\lim_{x \to \infty}$$
		$$\int \lim_a^b f(x)$$
		$$\int \int_V f(x) dx dy$$
		$$\iint_V f(x) dx \, dy$$

		$$\oplus_a^b $$
		$$\otimes_a^b $$
		$ a\, a\,\,\,  a a$
		this is italic \textit{htllo}\\
		this  is bold \textbf{hello}\\
		this is underline \underline{hello}\\
		hello world now I'm going to create a new paragraph.\par
		hell world this should be a new paragraph. Yes it is a new paragraph 
		indeed. It's cool yes it's really a cool thing.
		\begin{center}
			this should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong. this should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		tttthis should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong.his should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong.his should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong.his should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong.this should be placed center in pdf page. If not there is something 
		wrong. This should be placed center in pdf page. If not there is something 
		wrong.	wrong.
		\end{center}
\fi

	
\end{document}
